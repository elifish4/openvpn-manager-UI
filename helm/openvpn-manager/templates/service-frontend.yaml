apiVersion: v1
kind: Service
metadata:
  name: {{ include "openvpn-manager.frontend.name" . }}
  labels:
    {{- include "openvpn-manager.labels" . | nindent 4 }}
    {{- include "openvpn-manager.frontend.selectorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openvpn-manager.frontend.selectorLabels" . | nindent 4 }}
