apiVersion: v1
kind: Service
metadata:
  name: {{ include "openvpn-manager.backend.name" . }}
  labels:
    {{- include "openvpn-manager.labels" . | nindent 4 }}
    {{- include "openvpn-manager.backend.selectorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.backend.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openvpn-manager.backend.selectorLabels" . | nindent 4 }}
